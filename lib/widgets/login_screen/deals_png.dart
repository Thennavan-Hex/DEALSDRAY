import 'package:flutter/material.dart';

class DealsPng extends StatelessWidget {
  const DealsPng({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child:
        Image.asset('assets/deals_icon.png'),
      ),
    );
  }
}
